<%= form_with(model: size) do |form| %>
  <% if size.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(size.errors.count, "error") %> prohibited this size from being saved:</h2>

      <ul>
        <% size.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :fr_name, style: "display: block" %>
    <%= form.text_field :fr_name %>
  </div>

  <div>
    <%= form.label :eu_name, style: "display: block" %>
    <%= form.text_field :eu_name %>
  </div>

  <div>
    <%= form.label :us_name, style: "display: block" %>
    <%= form.text_field :us_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
